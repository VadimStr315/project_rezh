from django.conf.urls.static import static
from django.contrib import admin
from django.urls import path, include
import personal_area.views
from rezh import settings
from .views import *

urlpatterns = [
    # path('', vote, name='vote'),
]